-unless error_model.errors.empty?
  - model_name = model_name || error_model.class
  .alert.alert-danger
    %h4 Error creating #{indefinite_articlerize model_name.to_s.downcase }
    %p Please correct the following errors before continuing.
    %ul
    -if error_model
      -error_model.errors.full_messages.each do |msg|
        %li= msg